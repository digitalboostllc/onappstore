{
  "version": 2,
  "buildCommand": "export DATABASE_URL=\"$DATABASE_URL\" && export DIRECT_DATABASE_URL=\"$DIRECT_DATABASE_URL\" && prisma generate && prisma db push --accept-data-loss && next build",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "framework": "nextjs",
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  },
  "regions": ["iad1"]
} 